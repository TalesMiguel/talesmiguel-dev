// Tailwind v4: custom colors and fonts are configured via CSS @theme in global.css
// This file documents the design tokens used throughout the project.
// See src/styles/global.css for the actual @theme configuration.

/** @type {import('tailwindcss').Config} */
export default {
  // v4 auto-detects content — no need to specify paths
};
